## 1. Contagens da taxonomia

- [x] 1.1 Classificar cada ramo pela nota, usando as faixas de não atendido, parcialmente atendido e atendido e preservando `fulfilled` apenas como compatibilidade sem nota.
- [x] 1.2 Exibir as três contagens ao lado do título de cada taxonomia, preservando o controle de expansão.

## 2. Verificação

- [x] 2.1 Validar manualmente as contagens de ramos atendidos e não atendidos na análise detalhada do Oiac IA.
- [x] 2.2 Executar `pnpm lint`, `pnpm build` e `openspec validate add-taxonomy-evaluation-status --type change`.
